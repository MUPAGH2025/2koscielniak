name: sprawozdanie
on: push 

jobs:
  plot:
    runs-on: ubuntu-22.04
    steps:
      - run: sudo apt-get install texlive-xetex 
      - run: pip install scipy pint open_atmos_jupyter_utils matplotlib 
